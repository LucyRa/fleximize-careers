<section>
    <div class="container">
        <article>
            <h1>{!! $title !!}</h1>

            <p>
                {{ $blurb }}
            </p>
        </article>

        <img src="{{ $media }}" />
    </div>
</section>
